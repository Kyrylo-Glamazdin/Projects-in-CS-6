import React, {Component} from 'react';
import GifCard from './GifCard.js';

class GifList extends Component{
    constructor(props){
        super(props);
    }

    render(){
        return(
            <div/>
        );
    }
}